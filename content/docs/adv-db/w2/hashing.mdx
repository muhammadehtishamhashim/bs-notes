---
title: Hashing
description: Static and Dynamic Hashing Structures
weight: 4
---

import { ImageWrapper } from '@/components/mdx/image-wrapper';
import hashingDiagram from './files/hashingDiagram.png';

## Hashing: The "Direct Access" Shortcut

<div className="my-6 rounded-xl border border-primary/20 bg-primary/5 px-5 py-3 shadow-sm">
  <h3 className="text-lg font-bold text-primary mb-2 flex items-center gap-2">
    <svg className="w-5 h-5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="2"><path strokeLinecap="round" strokeLinejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg>
    How it works
  </h3>
  <div className="text-sm text-muted-foreground space-y-3">
    <div>
      It uses a mathematical formula called a Hash Function.
    </div>
    <div>
      <strong>The Magic:</strong> You give the ID to the formula, and it tells you exactly which "Bucket" (location) the data is in.
    </div>
  </div>
</div>

### Advantages & Disadvantages

| Feature | Description |
| :--- | :--- |
| **Instant Search (Pro)** | Finding a specific record (e.g., WHERE ID = 105) is nearly instant (O(1) speed). |
| **Bad for Ranges (Con)** | You cannot use Hashing to find "all names starting with A" or "salaries between 50k and 100k." |

### Deep Dive: Static vs Dynamic Hashing

### Static Hashing
In static hashing, when a search-key value is provided the hash function always computes the same address. The numbers of buckets provided remain the same at all times. 
*   **Insertion:** The hash function computes the bucket address for the search key where the record will be stored.
*   **Bucket Overflow:** The condition of bucket-overflow is known as a collision. This is a fatal state for any static hash function. To resolve this, the system uses **Overflow Chaining** (Closed Hashing), where a new bucket is allocated and linked after the previous one, or **Linear Probing** (Open Hashing), where the next free bucket is allocated.

### Dynamic Hashing
The problem with static hashing is that it does not expand or shrink dynamically as the size of the database grows or shrinks, leading to long overflow chains. Dynamic hashing provides a mechanism in which data buckets are added and removed dynamically and on-demand. 
*   **Extendible Hashing:** Uses a directory of pointers to buckets. It doubles the number of buckets by doubling the directory and splitting just the bucket that overflowed. 
*   **Linear Hashing:** Avoids the directory by using overflow pages and choosing buckets to split in a round-robin fashion.

<ImageWrapper 
  title="Static vs Dynamic Hashing"
  darkSrc={hashingDiagram}
  alt="A precise 2D technical computer science diagram comparing Static Hashing with Overflow Chaining versus Dynamic Hashing. The background must be pure dark mode (#111111). On the left (Static Hashing), show a fixed number of buckets with 'Overflow' chains (smaller attached blocks) hanging off one bucket that experienced a collision. On the right (Dynamic Hashing), show a directory structure indexing a dynamic set of buckets, with split buckets cleanly separated without long chains. Sleek, glowing neon-pink and cyan technical aesthetic." 
/> 

---