---
title: Charge, Materials & Ionization
description: Introduction to fundamental charge, atomic structure, and the process of ionization.
---

# Fundamental Charge

All matter is made up of atoms, which contain charged particles.

* **Electron ($-e$)**: Has a negative charge of $-1.6 \times 10^{-19} \text{ C}$
* **Proton ($+e$)**: Has a positive charge of $+1.6 \times 10^{-19} \text{ C}$
* **Nucleus**: The central part of the atom is positively charged (contains protons and neutrons).

The formula for total charge is:
$$
\begin{aligned}
Q &= \pm ne
\end{aligned}
$$
*(Where $n$ is an integer and $e$ is the elementary charge)*

## Material Classification

We can classify materials based on their atomic structure, specifically their **valence electrons** (electrons in the outermost shell):

* **Conductor**: Material where valence electrons are **less than 4**.
* **Insulator**: Material where valence electrons are **more than 4**.

<div className="my-8 grid grid-cols-1 md:grid-cols-2 gap-6 relative z-0">
  {/* Conductor Card */}
  <div className="rounded-xl border border-blue-500/20 bg-blue-500/5 p-6 flex flex-col items-center shadow-sm relative overflow-hidden group">
    <div className="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-blue-400 to-blue-600"></div>
    <h3 className="text-xl font-bold text-blue-500 mb-2">Conductor (e.g., Copper)</h3>
    <p className="text-sm text-muted-foreground mb-6 text-center">&lt; 4 Valence Electrons</p>
    
    <div className="relative w-32 h-32 flex items-center justify-center">
      {/* Nucleus */}
      <div className="w-12 h-12 rounded-full bg-blue-500 text-white flex items-center justify-center font-bold absolute z-10 shadow-[0_0_15px_rgba(59,130,246,0.6)]">
        Cu
      </div>
      {/* Outer Shell */}
      <div className="w-full h-full rounded-full border-2 border-dashed border-blue-500/30 animate-[spin_10s_linear_infinite]">
        {/* Valence Electron */}
        <div className="absolute top-0 left-1/2 -translate-x-1/2 -translate-y-1/2 w-4 h-4 rounded-full bg-blue-400 shadow-[0_0_8px_rgba(96,165,250,0.8)]"></div>
      </div>
    </div>
    <div className="mt-6 bg-background rounded p-2 text-xs font-mono text-center w-full border border-blue-500/20 text-blue-400">
      1 loosely bound valence electron
    </div>
  </div>

  {/* Insulator Card */}
  <div className="rounded-xl border border-red-500/20 bg-red-500/5 p-6 flex flex-col items-center shadow-sm relative overflow-hidden group">
    <div className="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-red-400 to-red-600"></div>
    <h3 className="text-xl font-bold text-red-500 mb-2">Insulator (e.g., Neon)</h3>
    <p className="text-sm text-muted-foreground mb-6 text-center">&gt; 4 Valence Electrons</p>
    
    <div className="relative w-32 h-32 flex items-center justify-center">
      {/* Nucleus */}
      <div className="w-12 h-12 rounded-full bg-red-500 text-white flex items-center justify-center font-bold absolute z-10 shadow-[0_0_15px_rgba(239,68,68,0.6)]">
        Ne
      </div>
      {/* Outer Shell */}
      <div className="w-full h-full rounded-full border-2 border-dashed border-red-500/30 animate-[spin_15s_linear_infinite]">
        {/* Valence Electrons (8 for Neon) */}
        {[...Array(8)].map((_, i) => (
          <div key={i} className="absolute top-1/2 left-1/2 w-4 h-4 rounded-full bg-red-400 shadow-[0_0_8px_rgba(248,113,113,0.8)]" 
               style={{ 
                 transform: `translate(-50%, -50%) rotate(${i * 45}deg) translateY(-64px)` 
               }}>
          </div>
        ))}
      </div>
    </div>
    <div className="mt-6 bg-background rounded p-2 text-xs font-mono text-center w-full border border-red-500/20 text-red-400">
      8 tightly bound valence electrons
    </div>
  </div>
</div>

---

## Atomic Example: Sodium (Na)

Let's look at a Sodium atom:
* **Mass No.**: 23
* **Atomic No.**: 11 (meaning 11 protons and 11 electrons in a neutral state)

<div className="my-8 flex justify-center relative z-0">
  <div className="relative w-64 h-64 flex items-center justify-center bg-card rounded-2xl border shadow-inner p-4">
    {/* Nucleus */}
    <div className="absolute w-12 h-12 rounded-full bg-red-500 text-white border-2 border-red-500 flex flex-col items-center justify-center font-bold z-10 shadow-[0_0_15px_rgba(239,68,68,0.6)]">
      <span>Na</span>
    </div>
    
    {/* Shell 1 (K) - 2 electrons */}
    <div className="absolute w-24 h-24 rounded-full border border-dashed border-primary/40 animate-[spin_8s_linear_infinite]">
      {[...Array(2)].map((_, i) => (
        <div key={i} className="absolute top-1/2 left-1/2 w-3 h-3 rounded-full bg-teal-400 shadow-[0_0_8px_rgba(45,212,191,0.8)]" 
             style={{ transform: `translate(-50%, -50%) rotate(${i * 180}deg) translateY(-48px)` }}>
        </div>
      ))}
    </div>

    {/* Shell 2 (L) - 8 electrons */}
    <div className="absolute w-40 h-40 rounded-full border border-dashed border-primary/40 animate-[spin_12s_linear_infinite_reverse]">
       {[...Array(8)].map((_, i) => (
          <div key={i} className="absolute top-1/2 left-1/2 w-3 h-3 rounded-full bg-teal-400 shadow-[0_0_8px_rgba(45,212,191,0.8)]" 
               style={{ transform: `translate(-50%, -50%) rotate(${i * 45}deg) translateY(-80px)` }}>
          </div>
        ))}
    </div>

    {/* Shell 3 (M) - 1 valence electron */}
    <div className="absolute w-56 h-56 rounded-full border-2 border-dashed border-primary/40 animate-[spin_20s_linear_infinite]">
      <div className="absolute top-1/2 left-1/2 w-4 h-4 rounded-full bg-teal-400 shadow-[0_0_12px_rgba(45,212,191,1)] border border-teal-200 ring-2 ring-teal-400/30"
           style={{ transform: `translate(-50%, -50%) translateY(-112px)` }}></div>
    </div>
  </div>
</div>

## Ionization

**Definition**: Removing or adding an electron from the outermost shell of an atom.

When this process occurs, the material becomes ionized. Let's compare a stable material to an ionized material using our Sodium example.

### 1. Stable Material (Neutral Na)
In a stable, neutral atom, the number of protons equals the number of electrons.
$$
\begin{aligned}
\text{Net Charge} &= -11e \text{ (electrons)} + 11e \text{ (protons)} \\
&= 0
\end{aligned}
$$

### 2. Ionized Material (Na+)
If the Sodium atom loses its outermost valence electron (1 electron leaves):
$$
\begin{aligned}
\text{Net Charge} &= +11e \text{ (protons)} - 10e \text{ (remaining electrons)} \\
&= +1e \text{ (net positive charge)}
\end{aligned}
$$

<div className="my-8 rounded-xl border bg-card overflow-hidden shadow-sm flex flex-col md:flex-row relative z-0">
  {/* Legend Panel */}
  <div className="w-full md:w-56 bg-muted/20 border-b md:border-b-0 md:border-r p-6 flex items-center justify-center md:items-start flex-col shrink-0 gap-4">
    <h4 className="font-bold text-lg text-foreground m-0 hidden md:block">Ionization</h4>
    <div className="flex flex-row md:flex-col gap-4 md:gap-3 flex-wrap justify-center">
      <div className="flex items-center gap-2">
        <div className="w-4 h-4 rounded-full bg-purple-500 shadow-sm border border-purple-700"></div>
        <span className="text-sm font-medium text-purple-600 dark:text-purple-400">Proton</span>
      </div>
      <div className="flex items-center gap-2">
        <div className="w-4 h-4 rounded-full bg-amber-500 shadow-sm border border-amber-700"></div>
        <span className="text-sm font-medium text-amber-600 dark:text-amber-400">Neutron</span>
      </div>
      <div className="flex items-center gap-2">
        <div className="w-4 h-4 rounded-full bg-teal-400 shadow-[0_0_6px_rgba(45,212,191,0.8)]"></div>
        <span className="text-sm font-medium text-teal-600 dark:text-teal-400">Electron</span>
      </div>
      <div className="flex items-center gap-2 md:mt-2 md:pt-3 md:border-t border-border/50">
        <svg className="w-5 h-5 text-orange-500" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        <span className="text-sm font-medium text-orange-600 dark:text-orange-400">Energy</span>
      </div>
    </div>
  </div>

  {/* Main Atom Visualization */}
  <div className="flex-1 p-8 md:p-12 flex items-center justify-center relative min-h-[350px] overflow-hidden bg-background/50">
    
    {/* Energy Input Arrow */}
    <div className="absolute left-2 md:left-8 top-1/2 -translate-y-1/2 flex items-center gap-2 text-orange-500 z-10 animate-pulse">
      <span className="font-bold uppercase tracking-wider text-xs hidden sm:block">Energy</span>
      <svg className="w-12 h-6 md:w-16 md:h-8" viewBox="0 0 48 24" fill="none" stroke="currentColor" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round">
        <path d="M0 12h40M30 4l10 8-10 8"/>
      </svg>
    </div>

    {/* The Atom Container (Sodium Match) */}
    <div className="relative w-[280px] h-[280px] flex items-center justify-center">
      
      {/* Shell 1 (K) - 2 electrons */}
      <div className="absolute w-24 h-24 rounded-full border border-dashed border-primary/20 animate-[spin_8s_linear_infinite]">
        {[...Array(2)].map((_, i) => (
          <div key={i} className="absolute top-1/2 left-1/2 w-3 h-3 rounded-full bg-teal-400 shadow-[0_0_8px_rgba(45,212,191,0.8)]" 
               style={{ transform: `translate(-50%, -50%) rotate(${i * 180}deg) translateY(-48px)` }}>
          </div>
        ))}
      </div>

      {/* Shell 2 (L) - 8 electrons */}
      <div className="absolute w-40 h-40 rounded-full border border-dashed border-primary/20 animate-[spin_12s_linear_infinite_reverse]">
         {[...Array(8)].map((_, i) => (
            <div key={i} className="absolute top-1/2 left-1/2 w-3 h-3 rounded-full bg-teal-400 shadow-[0_0_8px_rgba(45,212,191,0.8)]" 
                 style={{ transform: `translate(-50%, -50%) rotate(${i * 45}deg) translateY(-80px)` }}>
            </div>
          ))}
      </div>

      {/* Shell 3 (M) - The missing electron */}
      <div className="absolute w-56 h-56 rounded-full border-2 border-dashed border-primary/20 animate-[spin_20s_linear_infinite]">
        {/* Placeholder for the missing electron on the orbit */}
        <div className="absolute top-1/2 left-1/2 w-4 h-4 rounded-full border-2 border-orange-500 shadow-[0_0_15px_rgba(249,115,22,1)] animate-pulse"
             style={{ transform: `translate(-50%, -50%) rotate(45deg) translateY(-112px)` }}></div>
      </div>
      
      {/* The Nucleus (Sodium Match: 11 Protons, 12 Neutrons) */}
      <div className="absolute z-10 w-12 h-12 flex items-center justify-center">
        {/* Visual representation of a dense nucleus */}
        <div className="absolute top-[20%] left-[30%] w-3.5 h-3.5 rounded-full bg-purple-500 shadow-sm border border-black/20"></div>
        <div className="absolute top-[35%] left-[15%] w-3.5 h-3.5 rounded-full bg-amber-500 shadow-sm border border-black/20"></div>
        <div className="absolute top-[45%] left-[45%] w-3.5 h-3.5 rounded-full bg-purple-500 shadow-sm border border-black/20 z-10"></div>
        <div className="absolute top-[15%] left-[55%] w-3.5 h-3.5 rounded-full bg-amber-500 shadow-sm border border-black/20"></div>
        <div className="absolute top-[65%] left-[25%] w-3.5 h-3.5 rounded-full bg-purple-500 shadow-sm border border-black/20"></div>
        <div className="absolute top-[35%] left-[70%] w-3.5 h-3.5 rounded-full bg-amber-500 shadow-sm border border-black/20"></div>
        <div className="absolute top-[65%] left-[60%] w-3.5 h-3.5 rounded-full bg-purple-500 shadow-sm border border-black/20"></div>
        <div className="absolute top-[20%] left-[40%] w-3.5 h-3.5 rounded-full bg-amber-500 shadow-sm border border-black/20 z-10"></div>
        <div className="absolute top-[50%] left-[25%] w-3.5 h-3.5 rounded-full bg-amber-500 shadow-sm border border-black/20"></div>
        <div className="absolute top-[25%] left-[65%] w-3.5 h-3.5 rounded-full bg-purple-500 shadow-sm border border-black/20"></div>
        <div className="absolute top-[55%] left-[40%] w-3.5 h-3.5 rounded-full bg-amber-500 shadow-sm border border-black/20 z-10"></div>
        <span className="absolute z-20 text-[10px] font-bold text-white bg-black/40 px-1 rounded backdrop-blur-[2px]">Na+</span>
      </div>

      {/* Ejection Effect & Free Electron */}
      <div className="absolute z-20" style={{ left: '260px', top: '20px' }}>
        {/* Dashed Line back to Shell 3 */}
        <svg className="absolute top-0 left-0 overflow-visible text-orange-500 z-0 opacity-70">
           <path d="M -40 40 L -10 10" fill="none" stroke="currentColor" strokeWidth="2.5" strokeDasharray="6 4" />
        </svg>

        {/* Free Electron */}
        <div className="absolute z-10 w-5 h-5 rounded-full bg-teal-400 shadow-[0_0_12px_rgba(45,212,191,1)] flex items-center justify-center font-bold text-teal-950" style={{ transform: 'translate(-50%, -50%)' }}>
           <span className="text-[10px]">-</span>
        </div>
        <span className="absolute text-xs md:text-sm font-bold text-teal-600 dark:text-teal-400 whitespace-nowrap" style={{ left: '15px', top: '-10px' }}>Free Electron</span>
      </div>

    </div>
  </div>
</div>