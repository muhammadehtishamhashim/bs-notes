---
title: Information Representation
description: How computers store information using bits and bytes.
---

## How Computers Store Information

Computers are electronic devices that operate in two states: **ON** (high voltage) and **OFF** (low voltage). We represent these states mathematically using the binary system.

* **Bit (Binary Digit):** The smallest unit of data (0 or 1).
* **Byte:** A group of 8 bits. This is the standard unit for addressing memory.

### The Power of Abstraction
We don't usually read raw bits. We group them to represent larger numbers, characters, or instructions.

---

## Visualizing Memory

Below is a visualization of how the character **'A'** is stored in a single byte of memory using the ASCII standard (Value: 65).

<div className="my-6 p-6 border rounded-xl bg-gradient-to-r from-slate-900 to-slate-800 text-white shadow-lg">
  <div className="flex flex-col items-center">
    <h3 className="text-xl font-bold mb-4 text-blue-300">Memory Address: 0x004F</h3>
    
    {/* The Byte Representation */}
    <div className="flex gap-1 md:gap-2 mb-4">
      {[0, 1, 0, 0, 0, 0, 0, 1].map((bit, index) => (
        <div key={index} className={`
          w-8 h-12 md:w-12 md:h-16 flex items-center justify-center rounded-md font-mono text-xl font-bold border-2 transition-all hover:scale-110
          ${bit === 1 
            ? 'bg-blue-500 border-blue-400 shadow-[0_0_10px_rgba(59,130,246,0.5)]' 
            : 'bg-slate-700 border-slate-600 text-slate-400'}
        `}>
          {bit}
        </div>
      ))}
    </div>



    {/* Interpretation */}
    <div className="mt-6 grid grid-cols-3 gap-4 w-full text-center">
      <div className="bg-slate-700/50 p-3 rounded-lg">
        <div className="text-xs text-slate-400 uppercase tracking-wider">Decimal</div>
        <div className="text-2xl font-bold text-green-400">65</div>
      </div>
      <div className="bg-slate-700/50 p-3 rounded-lg">
        <div className="text-xs text-slate-400 uppercase tracking-wider">Hex</div>
        <div className="text-2xl font-bold text-yellow-400">0x41</div>
      </div>
      <div className="bg-slate-700/50 p-3 rounded-lg">
        <div className="text-xs text-slate-400 uppercase tracking-wider">ASCII</div>
        <div className="text-2xl font-bold text-pink-400">'A'</div>
      </div>
    </div>
  </div>
</div>



## Visualizing "Hello"

<div className="my-6 p-4 md:p-8 border rounded-xl bg-slate-900 text-white shadow-lg overflow-x-auto">
  <div className="flex gap-4 min-w-max justify-center">
    {[
      { char: 'H', decimal: 72, binary: '01001000', color: 'text-red-400', border: 'border-red-500/50', bg: 'bg-red-500/10' },
      { char: 'e', decimal: 101, binary: '01100101', color: 'text-green-400', border: 'border-green-500/50', bg: 'bg-green-500/10' },
      { char: 'l', decimal: 108, binary: '01101100', color: 'text-blue-400', border: 'border-blue-500/50', bg: 'bg-blue-500/10' },
      { char: 'l', decimal: 108, binary: '01101100', color: 'text-purple-400', border: 'border-purple-500/50', bg: 'bg-purple-500/10' },
      { char: 'o', decimal: 111, binary: '01101111', color: 'text-orange-400', border: 'border-orange-500/50', bg: 'bg-orange-500/10' },
    ].map((item, index) => (
      <div key={index} className={`flex flex-col items-center p-3 md:p-4 rounded-lg border-2 ${item.border} ${item.bg} transition-transform hover:scale-105`}>
        {/* Character */}
        <div className={`text-3xl md:text-5xl font-bold mb-2 ${item.color} font-mono`}>
          '{item.char}'
        </div>
        
        {/* Decimal/ASCII */}
        <div className="text-xs text-slate-400 uppercase tracking-widest mb-1">ASCII</div>
        <div className="text-lg md:text-xl font-bold text-white mb-3">{item.decimal}</div>
        
        {/* Binary */}
        <div className="text-xs text-slate-400 uppercase tracking-widest mb-1">Binary</div>
        <div className="font-mono text-xs md:text-sm bg-slate-950 px-2 py-1 rounded text-slate-300">
          {item.binary}
        </div>
      </div>
    ))}
  </div>
   <div className="text-center text-slate-400 mt-6 text-sm">
    Each character takes up <strong>1 Byte</strong> (8 bits) of memory.
  </div>
</div>

### Why 8 bits?
Historically, 8 bits were sufficient to store a single character of text (English alphabet, numbers, and punctuation) using the ASCII encoding standard.