---
title : Chinese Remainder Theorem
description: Solving a system of simultaneous congruent equations with different, coprime moduli.
---

import { MathWrapper } from '@/components/mdx/math-wrapper';
import { Callout } from 'fumadocs-ui/components/callout';

# Chinese Remainder Theorem (CRT)

The **Chinese Remainder Theorem (CRT)** is a powerful mathematical theorem used to find a unique solution to a set of different congruent equations with one variable ($X$), provided that their moduli are relatively prime (coprime) to each other.

### The Problem Structure

You are given a system of equations in the form:
$$ X \equiv a_1 \pmod{m_1} $$
$$ X \equiv a_2 \pmod{m_2} $$
$$ \dots $$
$$ X \equiv a_n \pmod{m_n} $$

*CRT states that this system has a unique solution modulo $M$ (where $M$ is the product of all individual moduli) IF and ONLY IF the moduli ($m_1, m_2, \dots, m_n$) are relatively prime pairwise.*

<MathWrapper title="The Master CRT Formula">
$$ X = (a_1 M_1 M_1^{-1} + a_2 M_2 M_2^{-1} + \dots + a_n M_n M_n^{-1}) \pmod M $$
</MathWrapper>

---

## 4-Step Resolution Algorithm

To solve a CRT problem, follow these precise steps:

<div className="space-y-4 my-6">
  <div className="flex gap-4 items-start bg-card border border-border/50 p-4 rounded-xl shadow-sm">
    <div className="bg-primary/10 text-primary font-bold px-3 py-1 rounded border border-primary/20">1</div>
    <div>
      <h4 className="font-bold text-foreground">Calculate Global Modulus ($M$)</h4>
      <p className="text-sm text-muted-foreground">Multiply all the small moduli together: $M = m_1 \times m_2 \times \dots \times m_n$.</p>
    </div>
  </div>
  <div className="flex gap-4 items-start bg-card border border-border/50 p-4 rounded-xl shadow-sm">
    <div className="bg-primary/10 text-primary font-bold px-3 py-1 rounded border border-primary/20">2</div>
    <div>
      <h4 className="font-bold text-foreground">Calculate Partial Moduli ($M_i$)</h4>
      <p className="text-sm text-muted-foreground">For each equation, divide the Global Modulus by its specific small modulus: $M_i = \frac{M}{m_i}$.</p>
    </div>
  </div>
  <div className="flex gap-4 items-start bg-card border border-border/50 p-4 rounded-xl shadow-sm">
    <div className="bg-primary/10 text-primary font-bold px-3 py-1 rounded border border-primary/20">3</div>
    <div>
      <h4 className="font-bold text-foreground">Find Modular Inverses ($M_i^{-1}$)</h4>
      <p className="text-sm text-muted-foreground">For each equation, solve for the inverse of $M_i$ under its original small modulo: $M_i \times M_i^{-1} \equiv 1 \pmod{m_i}$.</p>
    </div>
  </div>
  <div className="flex gap-4 items-start bg-card border border-border/50 p-4 rounded-xl shadow-sm">
    <div className="bg-primary/10 text-primary font-bold px-3 py-1 rounded border border-primary/20">4</div>
    <div>
      <h4 className="font-bold text-foreground">Apply the Master Formula</h4>
      <p className="text-sm text-muted-foreground">Plug all variables ($a_i, M_i, M_i^{-1}$) into the master formula and sum them up. Take the final result modulo $M$.</p>
    </div>
  </div>
</div>

---

## Applied Examples

### Example 1: Two Equations

Solve the following system for $X$:
1. $X \equiv 1 \pmod 4$
2. $X \equiv 3 \pmod 5$

**Extracted Variables:**
* $a_1 = 1, m_1 = 4$
* $a_2 = 3, m_2 = 5$

#### Process:
1. **$M$:** $M = 4 \times 5 = \mathbf{20}$
2. **$M_i$:** * $M_1 = \frac{20}{4} = \mathbf{5}$
   * $M_2 = \frac{20}{5} = \mathbf{4}$
3. **Inverses ($M_i^{-1}$):**
   * $5 \times M_1^{-1} \equiv 1 \pmod 4 \implies 1 \times M_1^{-1} \equiv 1 \pmod 4 \implies \mathbf{M_1^{-1} = 1}$
   * $4 \times M_2^{-1} \equiv 1 \pmod 5 \implies \mathbf{M_2^{-1} = 4}$ *(Since $4 \times 4 = 16 \equiv 1 \pmod 5$)*
4. **Formula:**
   $X = (1 \times 5 \times 1 + 3 \times 4 \times 4) \pmod{20}$
   $X = (5 + 48) \pmod{20}$
   $X = 53 \pmod{20}$

Subtract $20$ twice ($40$) to reduce the modulo:
$X = 53 - 40 = \mathbf{13}$

---

## Exam Critical Points
<div className="bg-red-500/5 border-l-4 border-red-500 p-4 rounded-r-xl my-6">
  <ul className="list-disc pl-5 space-y-2 text-sm font-medium text-foreground">
    <li><strong className="text-red-500">The Coprime Condition:</strong> The CRT is ONLY applicable if all moduli ($m_1, m_2, \dots$) are pairwise relatively prime (e.g., $\text{GCD}(m_1, m_2) = 1$).</li>
    <li><strong className="text-red-500">Modulo Reduction:</strong> Never forget the final step! The master formula yields a massive sum, but the final answer <strong>MUST</strong> be reduced using $\pmod M$.</li>
    <li>When finding the inverse $M_i^{-1}$, you must solve it relative to the <em>small</em> modulus $m_i$, NOT the global modulus $M$.</li>
  </ul>
</div>

## Comprehensive Summary
* **What it is:** A mathematical theorem ensuring a single unique integer solution exists for a system of multiple modulo equations.
* **Core Mechanism:** It breaks the system into sub-problems by isolating each modulus, calculating a partial block ($M_i$), determining its modular inverse, and mapping it back to the original remainders ($a_i$) to construct a unified result.
* **Significance:** CRT allows massive computational operations to be broken down into smaller, parallel modular computations, vastly speeding up cryptographic functions like RSA decryption.