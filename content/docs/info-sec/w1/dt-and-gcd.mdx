---
title: "Divisibility Theory and GCD"
description: "Understanding remainders, factors, and calculating Greatest Common Divisors."
---

import { Callout } from 'fumadocs-ui/components/callout';

# Divisibility Theory and GCD

Before learning complex encryption algorithms, we must master the basics of how numbers divide into one another.

## Divisibility

In cryptography and number theory, **Divisibility** strictly refers to a division operation where the **remainder equals exactly 0**. 

In programming, this is heavily reliant on the modulo operator (`%`), which returns the remainder of a division. If `A % B == 0`, then $A$ is divisible by $B$.

---

## Greatest Common Divisor (GCD) / HCF

The Greatest Common Divisor (GCD), also known as the Highest Common Factor (HCF), is the largest positive integer that perfectly divides two or more numbers without leaving a remainder.

Here are three basic methods to find the GCD before moving to advanced algorithms.

### Method 1: Listing Factors
The simplest way is to list all factors and find the highest common one.
**Example: Find GCD of 12 and 33**
* Factors of $12 = 1, 2, \mathbf{3}, 4, 6, 12$
* Factors of $33 = 1, \mathbf{3}, 11, 33$
* **Result:** $\text{GCD}(12, 33) = \mathbf{3}$

### Method 2: Prime Factorization
Break both numbers down into their prime components and multiply the shared primes.
**Example: Find GCD of 25 and 150**
* $25 = \mathbf{5 \times 5}$
* $150 = 2 \times 3 \times \mathbf{5 \times 5}$
* Common Primes $= 5 \times 5$
* **Result:** $\text{GCD}(25, 150) = \mathbf{25}$

### Method 3: Prime Number Rule (Coprimes)
If both numbers provided are prime numbers, they share no common factors other than 1.
**Example: Find GCD of 13 and 31**
* 13 is Prime.
* 31 is Prime.
* **Result:** $\text{GCD}(13, 31) = \mathbf{1}$

<Callout title="Coprime Numbers" type="info">
Whenever the $\text{GCD}$ of two numbers is exactly $1$, those numbers are referred to as **Coprime** or mutually prime. They do not have to be prime numbers themselves (e.g., $8$ and $9$ are coprime because $\text{GCD}(8,9) = 1$).
</Callout>
---

## Prime Numbers

A **Prime Number** is a number greater than $1$ that has exactly two factors: **$1$ and itself**. It cannot be divided further by any other number without leaving a remainder.

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 my-6">
  <div className="bg-card border border-border/50 rounded-xl overflow-hidden shadow-sm">
    <div className="bg-primary/10 p-3 border-b border-border/50">
      <h4 className="font-bold text-primary">Fast Facts About Primes</h4>
    </div>
    <ul className="p-4 space-y-2 text-sm text-muted-foreground list-disc pl-8">
      <li>The number **$2$** is the smallest prime number.</li>
      <li>The number **$2$** is the *only* even prime number.</li>
      <li>The number **$1$** is NOT a prime number.</li>
      <li>Except for $2$ and $5$, all prime numbers end in the digits **$1, 3, 7, \text{or } 9$**.</li>
    </ul>
  </div>

  <div className="bg-card border border-border/50 rounded-xl overflow-hidden shadow-sm">
    <div className="bg-orange-500/10 p-3 border-b border-border/50">
      <h4 className="font-bold text-orange-500">Why are Primes Essential in Crypto?</h4>
    </div>
    <ul className="p-4 space-y-2 text-sm text-muted-foreground list-disc pl-8">
      <li>Many modern encryption algorithms (like RSA) are directly based on prime numbers.</li>
      <li>It is **very fast** for computers to multiply two large prime numbers together.</li>
      <li>It is **extremely computer-intensive** to reverse engineer (factoring a massive number back into its original primes is incredibly hard).</li>
    </ul>
  </div>
</div>

### Prime Factorization Examples

Every number is either a prime or can be broken down into prime factors.

| Number | Prime Factorization | Is Prime? |
| :--- | :--- | :--- |
| **10** | $2^1 \times 5^1$ (Factors: $2, 5$) | **No** |
| **11** | $1 \times 11^1$ (Factors: $1, 11$) | <span className="text-green-500 font-bold">Yes</span> |
| **37** | $1 \times 37^1$ (Factors: $1, 37$) | <span className="text-green-500 font-bold">Yes</span> |
| **100** | $2^2 \times 5^2$ (Factors: $2, 5$) | **No** |
| **308** | $2^2 \times 7^1 \times 11^1$ (Factors: $2, 7, 11$) | **No** |

---

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-8">
  <div className="bg-primary/5 border border-primary/20 rounded-xl p-6">
    <h3 className="text-lg font-bold text-primary mb-3 flex items-center gap-2">
      <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="2"><path strokeLinecap="round" strokeLinejoin="round" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
      Critical Points
    </h3>
    <ul className="space-y-2 text-sm text-muted-foreground list-disc pl-4 marker:text-primary">
      <li>**Primes** are the ultimate building blocks of numbers. If a number isn't prime, it is a composite of primes.</li>
      <li>The difficulty of **Prime Factorization** of massive numbers is what keeps modern RSA encryption secure.</li>
      <li>**Divisibility** implies the modulo is strictly $0$. If there is any remainder, it is not divisible.</li>
    </ul>
  </div>

  <div className="bg-orange-500/5 border border-orange-500/20 rounded-xl p-6">
    <h3 className="text-lg font-bold text-orange-500 mb-3 flex items-center gap-2">
      <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="2"><path strokeLinecap="round" strokeLinejoin="round" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01" /></svg>
      Exam Important
    </h3>
    <ul className="space-y-2 text-sm text-muted-foreground list-disc pl-4 marker:text-orange-500">
      <li>Remember that **$1$ is NOT a prime number**. The smallest prime is $2$.</li>
      <li>Be prepared to quickly find the GCD of small numbers using Prime Factorization.</li>
      <li>Understand that **Coprimes** are any two numbers whose $\text{GCD} = 1$. They do *not* have to be prime themselves (like $8$ and $9$).</li>
    </ul>
  </div>
</div>
