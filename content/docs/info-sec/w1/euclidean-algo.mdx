---
title: "Euclidean Theory and Algorithm"
description: "The tabular and programmatic methods for calculating GCD efficiently, along with the core proof."
---

import { MathWrapper } from '@/components/mdx/math-wrapper';
import { Callout } from 'fumadocs-ui/components/callout';

# Euclidean Theory and Algorithm

When dealing with massive numbers in cryptography, listing factors or finding prime trees is computationally impossible. The **Euclidean Algorithm** provides a lightning-fast mathematical approach to finding the Greatest Common Divisor (GCD).

<Callout title="Prerequisite Rule" type="warn">
Always arrange your numbers such that $A > B$ before starting the algorithm.
</Callout>

<Callout title="The Core Theorem" type="info">
For any two positive integers $a$ and $b$, where $a = bq + r$, a common divisor of $a$ and $b$ is also a common divisor of $b$ and $r$. Therefore:
$$ \text{GCD}(a, b) = \text{GCD}(b, r) $$
</Callout>

---

## Proof of the Algorithm

**To Prove:** For $a = bq + r$, any common divisor of $a$ and $b$ is also a common divisor of $b$ and $r$, and vice versa.

<div className="my-6 overflow-x-auto border border-border/50 rounded-xl bg-card shadow-sm p-6">
  <div className="flex flex-col gap-4">
    
    <div className="border-l-4 border-purple-500 pl-4">
      <h4 className="font-bold text-foreground mb-1">Part 1: Forward Proof</h4>
      <p className="text-sm text-muted-foreground mb-2">Let $c$ be a common divisor of $a$ and $b$. This means $a = cq_1$ and $b = cq_2$.</p>
      <div className="font-mono text-xs bg-black/10 dark:bg-black/50 p-2 rounded text-purple-400">
        r = a - bq <br/>
        r = (cq₁) - (cq₂)q <br/>
        r = c(q₁ - q₂q) &nbsp;&nbsp;&nbsp;&nbsp; ... (i)
      </div>
      <p className="text-sm text-muted-foreground mt-2">This shows $c \mid r$. Hence, $c$ is a common divisor of $b$ and $r$.</p>
    </div>

    <div className="border-l-4 border-blue-500 pl-4 mt-4">
      <h4 className="font-bold text-foreground mb-1">Part 2: Reverse Proof</h4>
      <p className="text-sm text-muted-foreground mb-2">Let $d$ be a common divisor of $b$ and $r$. This means $b = r_1d$ and $r = r_2d$.</p>
      <div className="font-mono text-xs bg-black/10 dark:bg-black/50 p-2 rounded text-blue-400">
        a = bq + r <br/>
        a = (r₁d)q + r₂d <br/>
        a = d(r₁q + r₂) &nbsp;&nbsp;&nbsp;&nbsp; ... (ii)
      </div>
      <p className="text-sm text-muted-foreground mt-2">This shows $d \mid a$. Thus, $d$ is a common divisor of $a$ and $b$.</p>
    </div>
    
  </div>
</div>

From `(i)` and `(ii)`, we conclude that the sets of common divisors are identical, meaning: 
$$ \text{GCD}(a, b) = \text{GCD}(b, r) $$

Because the remainder $r$ strictly decreases ($0 \leq r < b$) at each step, the sequence of remainders must eventually reach $0$ after a finite number of steps, guaranteeing termination.

---

## The Tabular Method (Q, A, B, R)

The algorithm repeatedly divides $A$ by $B$ to find a Quotient ($Q$) and a Remainder ($R$). 
For the next row, $A$ inherits the old $B$, and $B$ inherits the old $R$. This continues until the Remainder ($R$) becomes $0$. The last non-zero $B$ is your GCD.

### Example 1: GCD(33, 12)

<div className="overflow-x-auto my-6">
  <table className="w-full text-center border-collapse">
    <thead>
      <tr className="bg-primary/10 border-y border-border">
        <th className="py-3 font-bold text-primary">Quotient (Q)</th>
        <th className="py-3 font-bold">A</th>
        <th className="py-3 font-bold">B</th>
        <th className="py-3 font-bold text-orange-500">Remainder (R)</th>
      </tr>
    </thead>
    <tbody className="divide-y divide-border/50 bg-card font-mono text-sm">
      <tr>
        <td className="py-3">2</td>
        <td className="py-3">33</td>
        <td className="py-3 font-bold text-blue-400">12 ↙</td>
        <td className="py-3 font-bold text-orange-400">9 ↙</td>
      </tr>
      <tr>
        <td className="py-3">1</td>
        <td className="py-3">12</td>
        <td className="py-3 font-bold text-blue-400">9 ↙</td>
        <td className="py-3 font-bold text-orange-400">3 ↙</td>
      </tr>
      <tr>
        <td className="py-3">3</td>
        <td className="py-3">9</td>
        <td className="py-3 font-bold text-green-500 bg-green-500/10 rounded">3</td>
        <td className="py-3 font-bold text-red-500">0</td>
      </tr>
      <tr className="text-muted-foreground bg-muted/20">
        <td className="py-3">X</td>
        <td className="py-3">3</td>
        <td className="py-3">0</td>
        <td className="py-3">X</td>
      </tr>
    </tbody>
  </table>
  <div className="mt-2 text-center text-sm font-bold text-green-500">Result: GCD(33, 12) = 3</div>
</div>

### Example 2: GCD(1005, 105)

<div className="overflow-x-auto my-6">
  <table className="w-full text-center border-collapse">
    <thead>
      <tr className="bg-primary/10 border-y border-border">
        <th className="py-3 font-bold text-primary">Q</th>
        <th className="py-3 font-bold">A</th>
        <th className="py-3 font-bold">B</th>
        <th className="py-3 font-bold text-orange-500">R</th>
      </tr>
    </thead>
    <tbody className="divide-y divide-border/50 bg-card font-mono text-sm">
      <tr><td className="py-2">9</td><td className="py-2">1005</td><td className="py-2">105</td><td className="py-2">60</td></tr>
      <tr><td className="py-2">1</td><td className="py-2">105</td><td className="py-2">60</td><td className="py-2">45</td></tr>
      <tr><td className="py-2">1</td><td className="py-2">60</td><td className="py-2">45</td><td className="py-2">15</td></tr>
      <tr><td className="py-2">3</td><td className="py-2">45</td><td className="py-2 font-bold text-green-500 bg-green-500/10 rounded">15</td><td className="py-2 font-bold text-red-500">0</td></tr>
      <tr className="text-muted-foreground bg-muted/20"><td className="py-2">X</td><td className="py-2">15</td><td className="py-2">0</td><td className="py-2">X</td></tr>
    </tbody>
  </table>
  <div className="mt-2 text-center text-sm font-bold text-green-500">Result: GCD(1005, 105) = 15</div>
</div>

---

## The Programmatic Algorithm

In programming languages, the Euclidean algorithm is incredibly easy to write using recursion and the modulo (`%`) operator.

The logic is defined mathematically as:
$$ \text{GCD}(a, b) = \text{GCD}(b, a \pmod b) $$

**Pseudocode Structure:**
```text
Function Euclid_GCD(a, b):
    If b == 0, then:
        return a
    Else:
        return Euclid_GCD(b, a % b)
```

### Algorithm Tracing Examples

**Example 1: Find GCD(50, 12)**
1. `GCD(50, 12)` $\implies$ `GCD(12, 50 % 12)` $\implies$ `GCD(12, 2)`
2. `GCD(12, 2)` $\implies$ `GCD(2, 12 % 2)` $\implies$ `GCD(2, 0)`
3. `GCD(2, 0)` $\implies$ Since $b=0$, return $a$, which is **$2$**.
* **Result:** **$2$**

---

## Practice Problems

Test your understanding of the algorithm. 

| Question | Problem | Answer |
| :--- | :--- | :--- |
| **Q1** | Find the GCD of $120$ and $45$ | **$15$** |
| **Q2** | Find the GCD of $144$ and $60$ | **$12$** |
| **Q3** | Find the GCD of $462$ and $192$ | **$6$** |
| **Q4** | Find the GCD of $84$ and $56$ | **$28$** |

---

## Exam Critical Points
<div className="bg-red-500/5 border-l-4 border-red-500 p-4 rounded-r-xl my-6">
  <ul className="list-disc pl-5 space-y-2 text-sm font-medium text-foreground">
    <li><strong className="text-red-500">Termination Condition:</strong> The algorithm strictly stops when the remainder ($R$) becomes $0$.</li>
    <li><strong className="text-red-500">The Result:</strong> The GCD is always the <strong>last non-zero remainder</strong> (or the value of $B$ right before $R$ becomes $0$).</li>
    <li><strong>Programmatic Rule:</strong> The recursive structure is $\text{GCD}(A, B) \rightarrow \text{GCD}(B, A \pmod B)$.</li>
    <li>Ensure $A \geq B$ before starting. If $B > A$, the first algorithmic step naturally swaps them (e.g., $12 \pmod{50} = 12$, causing the next step to be $\text{GCD}(50, 12)$).</li>
  </ul>
</div>

## Comprehensive Summary
* **What it is:** A recursive division algorithm based on Euclid's Division Lemma ($A = BQ + R$).
* **Core Mechanism:** It continuously shifts the divisor ($B$) into the dividend position ($A$), and the remainder ($R$) into the divisor position ($B$) until the remainder is zero. 
* **Why it matters:** Finding prime factors of 1000-digit numbers is physically impossible for modern computers. The Euclidean algorithm skips factorization entirely, finding the GCD using simple, fast division.