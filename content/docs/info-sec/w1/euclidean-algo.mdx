---
title: "Euclidean Theory and Algorithm"
description: "The tabular and programmatic methods for calculating GCD efficiently."
---

import { Callout } from 'fumadocs-ui/components/callout';

# Euclidean Theory and Algorithm

When dealing with massive numbers in cryptography, listing factors or finding prime trees is computationally impossible. The **Euclidean Algorithm** provides a lightning-fast mathematical approach to finding the Greatest Common Divisor (GCD).

<Callout title="Prerequisite Rule" type="warn">
Always arrange your numbers such that $A > B$ before starting the algorithm.
</Callout>

---

## The Tabular Method (Q, A, B, R)

The algorithm repeatedly divides $A$ by $B$ to find a Quotient ($Q$) and a Remainder ($R$). 
For the next row, $A$ inherits the old $B$, and $B$ inherits the old $R$. This continues until the Remainder ($R$) becomes $0$. The last non-zero $B$ is your GCD.

### Example 1: GCD(33, 12)

<div className="overflow-x-auto my-6">
  <table className="w-full text-center border-collapse">
    <thead>
      <tr className="bg-primary/10 border-y border-border">
        <th className="py-3 font-bold text-primary">Quotient (Q)</th>
        <th className="py-3 font-bold">A</th>
        <th className="py-3 font-bold">B</th>
        <th className="py-3 font-bold text-orange-500">Remainder (R)</th>
      </tr>
    </thead>
    <tbody className="divide-y divide-border/50 bg-card font-mono text-sm">
      <tr>
        <td className="py-3">2</td>
        <td className="py-3">33</td>
        <td className="py-3 font-bold text-blue-400">12 ↙</td>
        <td className="py-3 font-bold text-orange-400">9 ↙</td>
      </tr>
      <tr>
        <td className="py-3">1</td>
        <td className="py-3">12</td>
        <td className="py-3 font-bold text-blue-400">9 ↙</td>
        <td className="py-3 font-bold text-orange-400">3 ↙</td>
      </tr>
      <tr>
        <td className="py-3">3</td>
        <td className="py-3">9</td>
        <td className="py-3 font-bold text-green-500 bg-green-500/10 rounded">3</td>
        <td className="py-3 font-bold text-red-500">0</td>
      </tr>
      <tr className="text-muted-foreground bg-muted/20">
        <td className="py-3">X</td>
        <td className="py-3">3</td>
        <td className="py-3">0</td>
        <td className="py-3">X</td>
      </tr>
    </tbody>
  </table>
  <div className="mt-2 text-center text-sm font-bold text-green-500">Result: GCD(33, 12) = 3</div>
</div>

### Example 2: GCD(900, 750)

<div className="overflow-x-auto my-6">
  <table className="w-full text-center border-collapse">
    <thead>
      <tr className="bg-primary/10 border-y border-border">
        <th className="py-3 font-bold text-primary">Q</th>
        <th className="py-3 font-bold">A</th>
        <th className="py-3 font-bold">B</th>
        <th className="py-3 font-bold text-orange-500">R</th>
      </tr>
    </thead>
    <tbody className="divide-y divide-border/50 bg-card font-mono text-sm">
      <tr>
        <td className="py-3">1</td>
        <td className="py-3">900</td>
        <td className="py-3 font-bold text-blue-400">750 ↙</td>
        <td className="py-3 font-bold text-orange-400">150 ↙</td>
      </tr>
      <tr>
        <td className="py-3">5</td>
        <td className="py-3">750</td>
        <td className="py-3 font-bold text-green-500 bg-green-500/10 rounded">150</td>
        <td className="py-3 font-bold text-red-500">0</td>
      </tr>
      <tr className="text-muted-foreground bg-muted/20">
        <td className="py-3">X</td>
        <td className="py-3">150</td>
        <td className="py-3">0</td>
        <td className="py-3">X</td>
      </tr>
    </tbody>
  </table>
  <div className="mt-2 text-center text-sm font-bold text-green-500">Result: GCD(900, 750) = 150</div>
</div>

### Example 3: GCD(1005, 105)

<div className="overflow-x-auto my-6">
  <table className="w-full text-center border-collapse">
    <thead>
      <tr className="bg-primary/10 border-y border-border">
        <th className="py-3 font-bold text-primary">Q</th>
        <th className="py-3 font-bold">A</th>
        <th className="py-3 font-bold">B</th>
        <th className="py-3 font-bold text-orange-500">R</th>
      </tr>
    </thead>
    <tbody className="divide-y divide-border/50 bg-card font-mono text-sm">
      <tr><td className="py-2">9</td><td className="py-2">1005</td><td className="py-2">105</td><td className="py-2">60</td></tr>
      <tr><td className="py-2">1</td><td className="py-2">105</td><td className="py-2">60</td><td className="py-2">45</td></tr>
      <tr><td className="py-2">1</td><td className="py-2">60</td><td className="py-2">45</td><td className="py-2">15</td></tr>
      <tr><td className="py-2">3</td><td className="py-2">45</td><td className="py-2 font-bold text-green-500 bg-green-500/10 rounded">15</td><td className="py-2 font-bold text-red-500">0</td></tr>
      <tr className="text-muted-foreground bg-muted/20"><td className="py-2">X</td><td className="py-2">15</td><td className="py-2">0</td><td className="py-2">X</td></tr>
    </tbody>
  </table>
  <div className="mt-2 text-center text-sm font-bold text-green-500">Result: GCD(1005, 105) = 15</div>
</div>

---

## The Programmatic Algorithm

In programming languages, the Euclidean algorithm is incredibly easy to write using recursion and the modulo (`%`) operator.

The logic is defined mathematically as:
$$ \text{GCD}(a, b) = \text{GCD}(b, a \pmod b) $$

**Pseudocode Structure:**
```text
Function Euclid_GCD(a, b):
    If b == 0, then:
        return a
    Else:
        return Euclid_GCD(b, a % b)
```

### Algorithm Tracing Examples

**Example 1: Find GCD(50, 12)**
1. `GCD(50, 12)` $\implies$ `GCD(12, 50 % 12)` $\implies$ `GCD(12, 2)`
2. `GCD(12, 2)` $\implies$ `GCD(2, 12 % 2)` $\implies$ `GCD(2, 0)`
3. `GCD(2, 0)` $\implies$ Since $b=0$, return $a$, which is **$2$**.
* **Result:** **$2$**

**Example 2: Find GCD(83, 90)** *(Remember: $a$ must be $> b$, so flip them)*
1. `GCD(90, 83)` $\implies$ `GCD(83, 90 % 83)` $\implies$ `GCD(83, 7)`
2. `GCD(83, 7)` $\implies$ `GCD(7, 83 % 7)` $\implies$ `GCD(7, 6)`
3. `GCD(7, 6)` $\implies$ `GCD(6, 7 % 6)` $\implies$ `GCD(6, 1)`
4. `GCD(6, 1)` $\implies$ `GCD(1, 6 % 1)` $\implies$ `GCD(1, 0)`
5. `GCD(1, 0)` $\implies$ Since $b=0$, return $a$, which is **$1$**.
* **Result:** **$1$**
---

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-8">
  <div className="bg-primary/5 border border-primary/20 rounded-xl p-6">
    <h3 className="text-lg font-bold text-primary mb-3 flex items-center gap-2">
      <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="2"><path strokeLinecap="round" strokeLinejoin="round" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
      Critical Points
    </h3>
    <ul className="space-y-2 text-sm text-muted-foreground list-disc pl-4 marker:text-primary">
      <li>The **Euclidean Algorithm** is vastly more efficient than prime factorization, especially for numbers with hundreds of digits.</li>
      <li>The core repetition cycle is always: $A$ becomes $B$, and $B$ becomes the Remainder $R$.</li>
      <li>The algorithm always stops when the Remainder $R$ reaches exactly $0$. The non-zero $B$ on the step before $R=0$ is your result.</li>
    </ul>
  </div>

  <div className="bg-orange-500/5 border border-orange-500/20 rounded-xl p-6">
    <h3 className="text-lg font-bold text-orange-500 mb-3 flex items-center gap-2">
      <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="2"><path strokeLinecap="round" strokeLinejoin="round" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01" /></svg>
      Exam Important
    </h3>
    <ul className="space-y-2 text-sm text-muted-foreground list-disc pl-4 marker:text-orange-500">
      <li>Always ensure **$A > B$** before starting a manual calculation. If $B > A$, simply swap their positions.</li>
      <li>Be prepared to quickly trace the algorithm recursively (e.g., $\text{GCD}(83, 90) \rightarrow \text{GCD}(90, 83) \rightarrow \text{GCD}(83, 7) \dots$).</li>
      <li>If the final $\text{GCD}$ is $1$, immediately recognize that the original two numbers are **coprimes**.</li>
    </ul>
  </div>
</div>
